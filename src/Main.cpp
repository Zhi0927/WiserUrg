#include "URGSensorObjectDetector.hpp"
#include "vector3d.hpp"
#include "HKYObject.hpp"
#include "UrgDevice.hpp"
#include "utils.hpp"


int main()
{
    //std::vector<RawObject> vec;
    //std::vector<vector3> cachedDirs = {vector3(0, 0, 0)};
    //std::map<std::string, float> objectMapWithDistance;

    //RawObject raw(cachedDirs);
    //vec.emplace_back(raw);

    //objectMapWithDistance[raw.m_guid] = 5;

    //auto temp = objectMapWithDistance.begin()->first;

    //bool a = vec[0].m_guid == temp;

    //std::cout << a << std::endl;

    return 0;
}

